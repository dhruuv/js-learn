const accId= 456;
let accName= "Savings";
var bankName="ICICI";
accNo=8293892;

// accId=23;  assignment to const value is not possible
console.log(accName);

//prefer using let instead of var becuase of issues in block scope and functional-scope

//const value remains fixed throughout

let name;

console.log(name);  // undefined